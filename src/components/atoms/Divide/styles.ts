import { StyleSheet } from "react-native";

export default () =>
  StyleSheet.create({
    divide: {
      width: "80%",
      height: 1.5,
      borderRadius: 50,
      alignSelf: "center"
    }
  });
